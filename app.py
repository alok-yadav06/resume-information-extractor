"""
app.py
------
Streamlit web application entry point for the Resume Information Extractor.

Run with:
    streamlit run app.py

UI flow:
    1. User uploads a PDF or DOCX file via the file uploader widget.
    2. The file is temporarily saved and passed to ResumeExtractor.extract().
    3. The resulting structured dictionary is displayed as formatted JSON.
    4. The user can download the JSON output.
"""

# TODO: Implement the Streamlit UI.
